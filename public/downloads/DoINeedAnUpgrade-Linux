#!/bin/bash
# DoINeedAnUpgrade - Self-extracting hardware scanner

TEMP_DIR=$(mktemp -d)
trap "rm -rf $TEMP_DIR" EXIT

BINARY="$TEMP_DIR/DoINeedAnUpgrade-Linux-bin"
tail -n +3 "$0" | base64 -d > "$BINARY"
chmod +x "$BINARY"
exec "$BINARY"
__BINARY_MARKER__
